import e from "express";
import { describe, it, expect, vi } from "vitest";
import { createApp } from "../app";


describe('Express App:', () => {
    // it('should return an "Express" instance', () => {
    //     const testExpressApp = createApp()

    //     expect(testExpressApp).toBe()
    // });
})